/** 
 * 
 * 
Tuples

Typescript has new type called tuples. 
A tuple is a fixed length array where each element has a particular type.
We often use them when working with a pair of values.
Try and keep your tuples to values of two.
They are compiled as plain JavaScript arrays.

See example
**/

let user: [number, string] = [1, 'Mosh'];